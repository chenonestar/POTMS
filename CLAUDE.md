# POTMS 工程约定

因私出国（境）人员审批管理系统。给接手这个仓库的人（或 AI）看的操作规程。

需求、数据字典、设计取舍见 `系统需求与分析文档.html`（下称「文档」）。这里只写**动手时的规矩**，每条后面标它是被哪次返工换来的。

---

## 0. 这份文档找不到新问题

三轮返工共处理 25 条问题。四种手段各能找到什么：

| 手段 | 能找到 | 找不到 |
|---|---|---|
| 写文档 / 静态审查 / 自动化测试 | 实现有没有做到它打算做的事 | 意图本身对不对；自己的改动抽掉了什么 |
| **真人把页面点一遍** | 意图、提示、兜底页（命中 7/7、5/5） | 时区换算、跨记录唯一性这类点不出来的 |

**这份文档属于第一行。** 第三轮那 5 个新问题发生时，第 5 节的清单已有 18 条，其中**只有 1 条**可能产出它们，另外 17 条一条都产不出——而那 5 条确实全部由那 1 条产出。

比例是 **17 : 1**。所以：

- 清单打完勾 ≠ 查过了，只等于「这些老问题没复发」。
- 唯一产出新问题的动作放在第 4 节，是一道**工序**，不是清单里的一行。
- 「来自哪次返工」那一列说明：**没有对应返工的条目不存在**，清单一定有缺口。

清单不是天花板，是地板。

---

## 1. 系统与仓库

- **单用户、内网、完全离线、单机常驻**。无外部访问、无 API 消费方、无反向代理。前端不引 CDN。
- 同一套业务**实现了五遍**：Python（源头）、Go、Rust、.NET、Java，**共享同一个 `data.db`**。任何一版做出的破坏，其余四版都要承受。

**Python 是源头。** `database.py` 的 `SCHEMA` 常量是库结构唯一出处，生成另外两份：

```bash
python3 potms-dotnet/tools/gen-schema.py          # → Schema.cs
python3 potms-java/tools/gen-schema-java.py       # → schema.sql + Schema.java
python3 potms-dotnet/tools/gen-schema.py --check  # CI 跑的是 --check
```

改了 `SCHEMA` 不重新生成，CI 会红。Go / Rust 不吃生成物，按**列名**映射行——加列不会让它们崩，但它们不认新列，从那边建的记录该列恒为空。

```bash
pytest                       # 全量（pytest.ini 已带 -q）
pytest -p no:randomly        # 固定顺序，排查用例间干扰
python app.py                # 开发服务器，首次启动自动建库+迁移+灌字典
```

环境变量全部可选：`POTMS_BASE`（数据目录）、`POTMS_HOST` / `POTMS_PORT`、`POTMS_TZ_OFFSET`（**只管展示**，见 2.3）、`POTMS_REQUIRE_SIGNATURE`（默认 1，无手写板或补录历史时设 0）、`POTMS_DEBUG`（**生产绝不开**，调试页会摊开堆栈）、`POTMS_SM2_SOURCE`（仅 Java 版）。

---

## 2. 硬约束

### 2.1 运行时产物不许提交

`.secret_key` / `data.db*` / `backup/` / `uploads/` / `exports/` / `__pycache__/`，`.gitignore` 已覆盖。`data.db` 一度被误提交过，它会带着身份证号进版本库。CI 有一步守这个：测试跑完 `git status --porcelain` 必须为空——**往新目录写产物而没加 `.gitignore`，这一步就红**。

### 2.2 所有输出口显式 UTF-8

构建机是 Windows（cp1252），开发机是 Linux。本地全绿、CI 炸掉。为此栽过**三次**，规则从「文件」一路撑到「所有输出口」：

```python
open(path, encoding="utf-8")                 # 文件
sys.stdout.reconfigure(encoding="utf-8")     # 脚本开头（第三次就栽在 print 上）
subprocess.run(..., encoding="utf-8")        # 捕获输出时两头都要
```

### 2.3 两套时间口径

```
时间戳类  CURRENT_TIMESTAMP 存 UTC   → 展示与筛选按 DISPLAY_TZ_OFFSET_HOURS 换算
业务日期类 datetime.now()            → 直接取机器本地时钟，POTMS_TZ_OFFSET 管不到
```

- **凡是按日期筛选的 SQL，也必须先换算再取 `date()`。** 偏移量统一用 `helpers.tz_modifier()`，别手拼——手写 `'+'` 遇负偏移会拼出 `'+-5 hours'`，SQLite 不报错、只是悄悄忽略。漏换算的后果同样无声：本地当天 00:00–08:00 筛不出来，前一天同一时段反被错误命中。
- **部署约束：机器 OS 时区必须与 `POTMS_TZ_OFFSET` 一致**，否则每天过了 UTC 16:00 业务日期与展示时间差一天。已知欠账，现在不动。

---

## 3. 三组容易搞错的概念

搞错了写出来的判据是错的。

**台账 vs 单据。** `certificates` 是**台账**，跟人走、反映此刻事实；`cert_issuance` / `travel_details` / `decontrol_filing` 是**单据**，定格在开单那一刻。所以领用单上的 `cert_nos` 是固化的，后续改台账不影响这张签了字的凭证；`travel_details.unit` 与 `personnel_filing.work_unit` 不是冗余而是语义不同——**别加约束把两者绑死**，那会打掉单据的定格语义。

**路径 A / B。** A 从保管处借已有的证（有领用单）；B 做证自办（无领用单，判据是**号码是否进了台账**）。路径 B 那本证存在于现实却不在台账里，单独算一档，不计入「在库」。

**四档对账恒等式。**

```
在库 + 借出未还 = 在控人员台账登记总本数
逾期 ⊆ 借出未还 ∪ 新办未入库
```

按**号码槽**算不按台账行算（一行最多三本），只算在控人员，路径 B 未入库的不在此列。**证件号码是「哪本实体证此刻在谁手上」的唯一凭据**——`lent_out_numbers()` 会滤掉空号码，那本证于是从借出账上消失、仍被算作在库，**而恒等式照样平，一处都不喊**。这就是领用 `cert_nos` 必填的理由。

---

## 4. 交付流程（三道工序）

**工序一：写代码 + 写测试。** 见第 5 节。

**工序二：撤销验证。** 每条新测试都要验证「撤掉修复会变红」，且**朝一个「看起来对」的错误方向改，不是整块删**——整块删往往让别的断言先炸，看起来红了其实没验到。有效的撤法：把 `t_start` 换成 `t_end`（合理的笔误）、从表单取值而不是从关联记录取（那字段表单上没有，判据**静默永不触发**）、顺序写反（哪个来源权威读代码看不出来）。撤销验证本身也要**撤得准**，撤坏了会红错地方。

**工序三：真人把页面点一遍 ★** 这是**唯一产出新问题的动作**，测试全绿、审查干净、文档写全都替代不了。要特意做这几件测试不会做的事：

- **真的传一个大文件** —— 413/405 这类兜底页只有真触发才知道长什么样。
- **什么都不改直接提交** —— 表单预填的值真填进去了吗？（领用页号码框看着满的、其实是空的。）
- **看提示文案然后照着做** —— 提示说的和代码执行的可能不是一条（「≤10MB」说的是单文件，实际限请求体总量）。
- **把两个日期填成不合逻辑的先后** —— 单字段校验都在，字段之间往往没人管。

**交付方式：** 分批做、每批停下报告、每批推 CI；**先分析报告等审定再动手**（两轮里各拦下一次错误方向）；混在缺陷清单里的**需求问题第一件事是拆出来**——缺陷是修，需求是定。

**分支与 CI：** 在功能分支上开发，**推 main 需要明确授权**。注意 `build-python.yml` 只在 push main 或有 PR 时触发——**推功能分支不跑任何 CI**（栽过：四批修复推了分支就以为完事，实际零构建）。`build-windows` 那段本地无法复现（PyInstaller + 起真 exe 打 HTTP），漏 hidden-import、`--add-data` 没打进去在 pytest 里一律测不出来——**新增模板/静态文件后必须让这段跑过**。

---

## 5. 交付自检清单

编号沿用文档 §15.2。「来自哪次返工」不是装饰：它说明每条都是血换来的，也说明**没有对应返工的条目不存在**。

### 测试

| # | 检查项 | 来自 |
|---|---|---|
| 1 | 每个新测试都验证过「撤掉修复会变红」吗？朝合理的错误方向改，不是整块删 | 全项目最有价值的一条 |
| 2 | 造的数据能分辨出对错吗？断言写对 ≠ 测到了 | `⊆` 的用例数据恰好相等，写成 `=` 也全绿 |
| 3 | 断言范围够窄吗？别拿整页 HTML 断言「不包含某人」 | 姓名在页面上到处都是 |
| 4 | 用了秒级精度字段判断「有没有改动」吗？ | `updated_at` 分辨不出同一秒的两次写入 |
| 15 | 断言是按结构/文字精确定位，还是拿字符窗口截 HTML？ | 窗口太宽框进邻格，**撤掉星号照样绿** |
| 16 | 每条「拦下」都配了反向对照吗？ | 没对照的话，判据写成**一律拦死**也能变绿 |
| 17 | fixture 造的数据，在真库里存得进去吗？ | **加约束时测试大面积变红，往往不是约束错了，是测试数据一直是假的** |
| 23 | 「某字符串<u>不在</u>页面上」这类断言，先确认真取到页面了吗？ | URL 写错取回 404 页，「旧名字不在」在 404 页上**永远成立**。同类坑第四次 |

> 第 23 条写成**可执行动作**（「先断言 `status_code == 200`」）而不是**提醒**。第 15 条写成提醒，结果又栽了一次。

### 判据

| # | 检查项 | 来自 |
|---|---|---|
| 5 | 同一判据只有一个实现吗？高亮、筛选、首页数字、导出必须共用一个函数 | 已撤控人员的排除差点只加在筛选一侧 |
| 12 | 新增路径加的每道校验，编辑路径问过「这里也该有吗」？ | 一轮撞两次。**写新增想的是别让脏数据进来，写编辑想的是别把用户填好的判成错——心态一翻，把关就松了** |
| 13 | 汇总数字与它点开的明细，是同一个函数出的吗？ | 卡上 1 本点进去 4 行；按钮灰了 POST 照样过。**出现最多的根因（4 次）** |
| 14 | 必填与否看的是「系统给不给得出这个值」，不是「表单好不好看」？ | 入库批号必填却无来源；批准日期选填却《审批表》必传 |
| 18 | 这条规则挡住坏情形，**顺带**挡住了什么？判据下沉到「这次操作成不成立」，别停在「这个对象还能不能动」 | 「行程已结束不许领用」把事后补录也堵死了 |
| 20 | 同一类判据，摆成一张表整体看过吗？不是「这条对不对」，是「这类还缺哪几条」 | 日期先后校验此前 4 条**没有一条是主动想出来的**；摆成表当场又找出 4 个缺口 |

### 改动

| # | 检查项 | 来自 |
|---|---|---|
| 6 | 改了一处，问过「还有哪里也是这样」吗？（找同类） | 同一编码根因一周内咬两次 |
| 19 | 这次改动**拿掉**了什么？有没有别处正靠着它？（找被自己抽走的地基） | 证件种类改成服务端预选是好事，却顺手抽掉了唯一会触发号码自动带入的那次 `change`。**两处代码各自都对，断的是一条没写在任何地方的隐式依赖** |
| 21 | 加了新字段，被它取代的旧字段收窄了吗？ | **补字段是上半场，退役旧字段是下半场**；只做上半场是把「没有答案」换成「有两个答案」，后者更难发现，因为它看起来是完整的 |
| 22 | 提示文案说的规则，和代码实际执行的是同一条吗？ | 「≤10MB」说的是单文件，实际限请求体总量。**给一句读起来合理、照做却失败的提示，比没有提示更糟** |

### 工程

| # | 检查项 | 来自 |
|---|---|---|
| 7 | 所有输出口都写了 `encoding="utf-8"` 吗？ | 见 2.2，栽过三次 |
| 8 | 改了 schema 就重新生成 .NET / Java 两份了吗？ | CI 有 `--check` 守着 |
| 9 | 真的打开浏览器看过吗？→ **见第 4 节工序三，那是一道工序，不是这表里的一行** | 多处连续被**人眼**发现，无一由测试或自审发现 |
| 10 | 动过未纳入版本控制的文件吗？改之前先备份，且**保留路径** | `git checkout` 救不回来；用 basename 会让 `travel/form.html` 和 `decontrol/form.html` 撞名 |
| 11 | 文档跟着改了吗？文档落后于代码就等于错 | 上一版需求文档正是这么烂掉的 |

---

## 6. 已知欠账

清单只有「应该做到」而没有「还没做到」，就成了自我表扬。完整列表见文档 §15.3，最要紧的：

- **高** —— 近三轮 11 批修复**只做了 Python 版**。五版共享同一个 `data.db`，从那四版登记的领用记录仍可能号码为空。
- **中** —— `release.yml`（425 行，五版一次出齐）**从没真正跑过一次**；盘库只能出清单，盘点结果回不到系统。
- 三处错误仍走框架默认页（英文、无导航、整页替换）：**405 真实可达**（21 条 POST-only 动作路由），400 非 CSRF、403/401 目前不可达但那一支敞着。

---

## 7. 三轮复盘，一句话

1. 坚持写实际的样子，写的过程就会撞见不一致——这是对着代码能拿到的收益。
2. 这个收益有上限：文档、审查、测试共用一个盲区，只能验证「实现与意图一致」。
3. 还有一层：**自己的修复会产生新的问题面**。上一轮那批改动测试全绿、撤销验证做全、反向对照配齐，它自己一点没错，错在没人问过「我这次改动，让哪些原本会发生的事不再发生了」。

> **跑得通、测得过、审得干净的系统，和对的系统，是两回事。**
